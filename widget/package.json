{
  "name": "certmate-agent-widget",
  "private": true,
  "version": "0.1.0",
  "description": "Vanilla web component for CertMate Agent. No build step — devDeps only for tests.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^4.1.0",
    "happy-dom": "^20.8.9"
  }
}
